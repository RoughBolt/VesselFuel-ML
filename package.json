{
  "name": "maritime-xai-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "balls": "concurrently -n \"BACKEND,FRONTEND\" -c \"bgBlue.bold,bgGreen.bold\" \"uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
